
--- MODEL OUTPUT ---
reset_password(user_email="<email>", method="email")

--- MODEL OUTPUT ---
I'd call the `reset_password` tool with the following parameters:

`reset_password(user_email="<email>", method="email")`

This will reset the user's password and send a password reset link to their email address, allowing them to regain access to their computer.

--- MODEL OUTPUT ---
Let's go through the questions:

Q1: Problem type? (auth/KB/outage/hardware/software/security/access/billing/history) → auth

Q2: Can user self-resolve with KB? YES → no (portal unreachable)

Q3: Possible known outage? "team-wide"/"server issue?"/"since this morning" → no

Q4: Security incident? phishing/suspicious link/malware/renamed files → no

Q5: Physical work needed? battery/RAM/screen upgrade → no

Based on the answers, I think this is an authentication issue, and the user is locked out of their account. Since they can't self-resolve with KB and there's no known outage or security incident, I'll use the `reset_password` tool.

Action: reset_password(user_email="<email>", method="email")

--- MODEL OUTPUT ---
Let's apply the diagnostic framework to this request:

Q1: What is the problem TYPE?
    AUTH — user cannot authenticate.

Q2: Can the user self-resolve with KB guidance?  YES → lookup_knowledge_base FIRST.

Q3: Could this be a KNOWN SERVICE OUTAGE?
    No, not a team-wide outage.

Q4: Is this a SECURITY INCIDENT?
    No, not a phishing or suspicious link.

Q5: PHYSICAL work needed / EXPLICIT upgrade request?
    No, no physical work required.

Based on the answers, I would:

→ reset_password directly.

Action: reset_password(user_email="<email>", method="email")

Please note that the user is unable to access the self-service portal, so I would not provide a KB link or ask them to try to reset their password through the portal. Instead, I would initiate the password reset process directly.

--- MODEL OUTPUT ---
reset_password(user_email="<email>", method="email")

--- MODEL OUTPUT ---
Based on the rules, I'll call the correct tool. Since the user is locked out of their computer, I'll use the `reset_password` tool.

Tool: `reset_password(user_email="<email>", method="email")`

This will reset the user's password and allow them to access their computer again.

--- MODEL OUTPUT ---
Let's break it down:

Q1: Problem type? → auth

Q2: Can user self-resolve with KB? → no (portal unreachable)

Q3: Possible known outage? → no

Q4: Security incident? → no

Q5: Physical work needed? → no

Based on the answers, I think this is a classic case of a user being locked out of their account and needing help to reset their password.

Action: reset_password(user_email="<email>", method="email")

I'll go ahead and reset the user's password and send them an email with the new password.

--- MODEL OUTPUT ---
Let's apply the diagnostic framework to this task.

**Q1: What is the problem TYPE?**
AUTH - the user is having an authentication issue.

**Q2: Can the user self-resolve with KB guidance?**
No - the user is already locked out and cannot access the self-service portal.

**Q3: Could this be a KNOWN SERVICE OUTAGE?**
No - there is no indication of a team-wide or service-wide issue.

**Q4: Is this a SECURITY INCIDENT?**
No - it's just a password reset request.

**Q5: PHYSICAL work needed / EXPLICIT upgrade request?**
No - no physical work or upgrade is required.

Based on the answers, I will call the correct tool:
**reset_password(user_email="<email>", method="manual")**

This will initiate the password reset process for the user, and I will guide them through the process to regain access to their computer.
